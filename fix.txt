The issue is on line 627:
Current: )}
Should be: }

The regular layout section should close with ) before the }, but it seems like there's a structural issue where the regular layout is not properly closed.

The structure should be:
{isTheaterMode ? (
  // theater content
) : (
  // regular content  
)}

But currently it seems like:
{isTheaterMode ? (
  // theater content
) : (
  // regular content - missing closing )
}

Need to add the missing ) for the regular layout section.